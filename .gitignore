# Node modules for both frontend and backend
/frontend/node_modules/
/backend/node_modules/

# Environment variables for both frontend and backend
# These contain sensitive information and should NEVER be committed to Git
/frontend/.env
/backend/.env
.env # In case you accidentally create one at the root

# Build output for frontend (React's production build)
/frontend/build/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
# These are generated by IDEs and should not be committed
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# macOS specific files
.DS_Store

# npm package lock v2 (optional, if you have one at root, though usually in subfolders)
package-lock.json
